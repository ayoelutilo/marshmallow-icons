{
  "buildCommand": "npm install && mkdir -p dist && npm -w marshmallow-icons run generate && npm -w @marshmallow/uds-docs run build && ls -la dist",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}



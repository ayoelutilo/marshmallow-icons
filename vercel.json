{
  "version": 2,
  "installCommand": "if [ -d apps/docs ]; then npm install; else cd ../.. && npm install; fi",
  "buildCommand": "project_dir=\"$PWD\"; if [ -d apps/docs ]; then npm run docs:build; else cd ../.. && npm run docs:build && rm -rf \"$project_dir/public\" && cp -R public \"$project_dir/public\"; fi",
  "outputDirectory": "public"
}


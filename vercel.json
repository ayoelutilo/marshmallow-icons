{
  "buildCommand": "npm install && npm -w marshmallow-icons run generate && npm -w @marshmallow/uds-docs run build && cd $(git rev-parse --show-toplevel 2>/dev/null || pwd) && node scripts/verify-build.mjs",
  "outputDirectory": "public",
  "installCommand": "npm install"
}



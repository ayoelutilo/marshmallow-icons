{
  "name": "marshmallow-unified-design-system",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "icons:generate": "npm -w marshmallow-icons run generate",
    "icons:build": "npm -w marshmallow-icons run build",
    "docs:dev": "npm run icons:generate && npm -w @marshmallow/uds-docs run dev",
    "docs:build": "npm run icons:generate && npm -w @marshmallow/uds-docs run build",
    "build": "cd packages/icons && npm run build && cd ../.. && npm run docs:build",
    "vercel-build": "npm run build"
  }
}


